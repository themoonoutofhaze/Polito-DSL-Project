This project involves the development of a multi-output pipeline for predicting the coordinates (x, y) of a
particle passing through a resistive silicon detector (RSD). The pipeline uses the XGBRegressor to analyze signals generated by
12 pads in the detector. The provided dataset contains properties of the signals produced by each pad and the effectiveness of the pipeline is evaluated using the mean Euclidean distance metric.
